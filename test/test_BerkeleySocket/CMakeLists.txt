SET(PACKAGE testBerkeleySocket)

FILE(GLOB PACKAGE_SRC "*")
#source_group(test\\${PACKAGE} FILES ${PACKAGE_SRC})
source_group(test FILES ${PACKAGE_SRC})

ADD_EXECUTABLE(${PACKAGE} ${PACKAGE_SRC})
TARGET_LINK_LIBRARIES(${PACKAGE} ${EXT_LIB})

